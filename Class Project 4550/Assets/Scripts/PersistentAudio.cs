using UnityEngine;

public class AudioPersistence : MonoBehaviour
{
    private static AudioPersistence instance;

    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
